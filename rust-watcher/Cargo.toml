[package]
name = "aw-watcher-enhanced"
version = "1.0.0"
edition = "2021"
description = "Enhanced ActivityWatch watcher with rich context capture"
license = "MIT"

[dependencies]
# ActivityWatch client (HTTP API)
reqwest = { version = "0.12", features = ["json", "blocking"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
gethostname = "0.5"

# Config
toml = "0.8"
dirs = "6"

# Logging
log = "0.4"
env_logger = "0.11"

# Error handling
thiserror = "2"
anyhow = "1"

# CLI
clap = { version = "4", features = ["derive"] }

# Signal handling
ctrlc = "3"

# Regex
regex = "1"

# URL parsing
url = "2"

# File watching
notify = "7"

# macOS-specific
[target.'cfg(target_os = "macos")'.dependencies]
core-foundation = "0.10"
core-foundation-sys = "0.8"
core-graphics = "0.24"

# Windows-specific
[target.'cfg(target_os = "windows")'.dependencies]
windows = { version = "0.58", features = [
    "Win32_Foundation",
    "Win32_System_ProcessStatus",
    "Win32_System_Threading",
    "Win32_System_SystemInformation",
    "Win32_UI_WindowsAndMessaging",
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_Graphics_Gdi",
    "Win32_Storage_Xps",  # PrintWindow lives here in windows-rs bindings
    "Win32_System_WinRT",
    # WinRT bindings used by ocr.rs for Windows.Media.Ocr
    "Foundation",
    "Foundation_Collections",
    "Graphics_Imaging",
    "Storage_Streams",
    "Media_Ocr",
] }
